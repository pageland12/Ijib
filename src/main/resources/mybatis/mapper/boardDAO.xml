<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.springboot.ijib.dao.IBoardDAO">
	<select id="boardList" resultType="com.springboot.ijib.dto.BoardDTO">
		select b.*, m.mname
		 from board b
		 join member m on b.mno = m.mno
		 order by b.bno desc
	</select>
	
	<select id="boardView" resultType="com.springboot.ijib.dto.BoardDTO">
		select b.*, m.mname
		 from board b
		 join member m on b.mno = m.mno
		 where b.bno = #{bno}
	</select>
	
	<insert id="boardWrite">
		insert into board(bno, btitle, bcontent, bcategory, bdate, mno)
		 values(BOARD_SEQ.NEXTVAL, #{btitle}, #{bcontent}, #{bcategory}, SYSDATE, #{mno})
	</insert>
	
	<update id="boardUpdate">
		update board
		 set btitle = #{btitle}, bcontent = #{bcontent}, bcategory = #{bcategory}
		 where bno = #{bno}
	</update>
	
	<delete id="boardDelete">
		delete from board
		 where bno = #{bno}
	</delete>
	
	<update id="boardHit">
		update notice
		 set bhit = bhit + 1
		 where bno = #{bno}
	</update>
</mapper>