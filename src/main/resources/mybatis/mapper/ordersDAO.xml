<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.springboot.ijib.dao.IOrdersDAO">
	<!-- 회원용 주문 목록 조회 -->
	<select id="mordersList" resultType="com.springboot.ijib.dto.OrdersDTO">
		SELECT O.ONO, O.OPRICE, O.OPAYMENT, O.ODATE, P.PNAME
		  FROM ORDERS O
		 LEFT OUTER JOIN MEMBER_PASSES MP
		    ON O.ONO = MP.ONO
		 LEFT OUTER JOIN PASS P
		 	ON MP.PNO = P.PNO
		 WHERE O.MNO = #{mno}
		 ORDER BY O.ONO DESC
	</select>
	
	<!-- 주문 목록 등록 -->
	<insert id="orderInsert">
		INSERT INTO ORDERS(ONO, OPRICE, OPAYMENT, ODATE, MNO)
		 VALUES(#{ono}, #{oprice}, #{opayment}, SYSDATE, #{mno})
	</insert>
</mapper>