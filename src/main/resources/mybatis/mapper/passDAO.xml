<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.springboot.ijib.dao.IPassDAO">
	<select id="passList" resultType="com.springboot.ijib.dto.PassDTO">
		select *
		 from pass
		 order by pno asc
	</select>
	
	<select id="passView" resultType="com.springboot.ijib.dto.PassDTO">
		select *
		 from pass
		 where pno = #{pno}
	</select>
	
	<insert id="passWrite">
		insert into pass(pno, pimg, pname, pprice, pperiod)
		 values(PASS_SEQ.NEXTVAL, #{pimg}, #{pname}, #{pprice}, #{pperiod})
	</insert>
	
	<update id="passUpdate">
		update pass
		 set pimg=#{pimg}, pname=#{pname}, pprice=#{pprice}, pperiod=#{pperiod}
		 where pno=#{pno}
	</update>
	
	<delete id="passDelete">
		delete from pass
		 where pno=#{pno}
	</delete>
</mapper>
